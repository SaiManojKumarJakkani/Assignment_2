# Assignment_2
Use command "java -jar Assignment_2.java" to execute the application.

Then follow the instruction to fill the details for you LinkedIn profile application

At the end it asks the format in which the details should be saved, so give the approapriate format, the output files will be saved in the src/output_files/ directory
